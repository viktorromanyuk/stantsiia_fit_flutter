import 'package:flutter/material.dart';

class TrainingsNestedScreen extends StatelessWidget {
  const TrainingsNestedScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: const Text('Nested Trainings')),
  );
}
